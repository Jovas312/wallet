spring:
  application:
    name: wallet
  datasource:
    url: jdbc:postgresql://localhost:5432/wallet_db
    username: postgres
    password: Jov@ny23
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  flyway:
    enabled: true
    baseline-on-migrate: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.results.loading: TRACE

openpay:
  sandbox:
    base-url: https://sandbox-api.openpay.mx/v1
    merchant-id: mxipm8p3keuc9u4fpxzl
    private-key: sk_566e53d64c194ccc92684dc3a59d3141