spring:
  application:
    name: wallet
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/wallet_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:Jov@ny23}
    driver-class-name: org.postgresql.Driver
jwt:
  secret:
    key: ${SECRETKEY:da4e0b90d57579339b6920cf5c9bcc10f9fec7167ff4f3ef42cee94a8978e5ba}
  expiration:
    time: 86400000

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  flyway:
    enabled: true
    baseline-on-migrate: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.results.loading: TRACE

openpay:
  sandbox:
    base-url: https://sandbox-api.openpay.mx/v1
    merchant-id: mxipm8p3keuc9u4fpxzl
    private-key: sk_566e53d64c194ccc92684dc3a59d3141
